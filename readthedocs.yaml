version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"   # Ensure this matches the Python version for your project.

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt  # Make sure this file includes all necessary dependencies.
  version: 3.12  # Ensure this matches the Python version you want to use.
